# Legacy predictions

These records are preserved for comparison. Their `document_accuracy` fields are heuristic values, not measured accuracy, and some work lists include project events. Use `../model_parsed_1500_audited/` for the corrected batch output and `../accuracy_report.md` for the audit. The summary in this folder now counts actual stored statuses and explicitly withdraws the former accuracy claim.
